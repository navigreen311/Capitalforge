'use client';

// ============================================================
// /offboarding
//
// Both this and /platform/offboarding render the same view. They were two
// pages over the same subject with different fixtures, which is how one came
// to show a client's data as deleted while the other showed the same subject
// as still held.
// ============================================================

export { OffboardingView as default } from '@/components/offboarding/offboarding-view';
